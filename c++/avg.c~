#include <stdio.h>

int avg(int array[], int size);

int main(){
  int array[] = {1,2,3,4}
  avg = avg(array);
  printf("%d", avg);
  return 0;
}

int avg(int array[], int size){
  int i;
  avg = 0;
  for(i=0;i<size;i++){
    avg += array[i];
  }
  return avg;
}
